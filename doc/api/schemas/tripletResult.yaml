title: TripletResult
type: object
properties:
  triplets:
    title: Triplets
    description: |
      Triplets of reference, structure and taxon IDs
    type: array
    items:
      type: array
      items:
        type: integer
    example : [ [44488598, 27151406, 158572] ]
  references:
    description: |
      References
    type: object
    properties:
      ids:
        description: |
          IDs
        type: integer
        example: 44488598
      references:
        $ref: "./references.yaml"
  structures:
    description: |
      Structures
    type: object
    properties:
      ids:
        description: |
          IDs
        type: integer
        example: 27151406
      structures:
        $ref: "./structures.yaml"
  taxa:
    description: |
      Taxa
    type: object
    properties:
      ids:
        description: |
          IDs
        type: integer
        example: 158572
      taxa:
        $ref: "./taxa.yaml"
  count:
    description: |
      Count
    type: integer
  description:
    description: |
      Description
    type: string