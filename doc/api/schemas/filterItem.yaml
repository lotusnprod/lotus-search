title: FilterItem
type: object
properties:
  # reference:
  #   $ref: "./referenceFilterItem.yaml"
  structure:
    $ref: "./structureFilterItem.yaml"
  taxon:
    $ref: "./taxonFilterItem.yaml"
  limit:
    description: |
      Limit of the number of results.
    default: false
    example: 1000
    type: integer
