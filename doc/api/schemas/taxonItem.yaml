title: TaxonItem
type: object
properties:
  wid:
    description: |
      Wikidata identifier of the taxon (without the Q).
    default: ~
    example: 158572
    type: integer
  name:
    description: |
      The name searched (can be partial and slightly incorrect).
    default: ~
    # The mistake is on purpose
    example: "Gentiana luteha"
    type: string
  # external_id:
  #   $ref: "./taxonId.yaml"
  option:
    $ref: "./taxonOption.yaml"
  # filter:
  #   $ref: "./taxonFilter.yaml"
  # limit:
  #   $ref: "./limit.yaml"