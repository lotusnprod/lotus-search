post:
  operationId: searchTaxaByName
  security: []
  summary: |
    Search taxa by name matching
  description: |
    Search taxa by name matching. The search is case-insensitive and will return all taxa whose name contains the given string.
  requestBody:
    description: Query
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            name:
              type: string
              # The mistake is on purpose
              example: "Gentiana luteha"
              description: |
                The name searched (can be partial and slightly incorrect)
            with_compounds:
              type: boolean
              example: true
              description: |
                Whether to include the number of compounds associated with each taxon
  responses:
    '200':
      description: At least one taxon matching the given name was found
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/taxon_with_structure_count.yaml'
    '404':
      description: No taxon matching the given name could be found
      content:
          application/json:
              schema:
                type: object
                properties:
                    message:
                      type: string
                      example: No taxon matching the given name could be found
